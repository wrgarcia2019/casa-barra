# Frontend (Vite) environment variables
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_ADMIN_EMAIL=wrgarcia2003@gmail.com

# Optional: buckets used by Storage (defaults: hero/gallery)
VITE_SUPABASE_BUCKET_HERO=hero
VITE_SUPABASE_BUCKET_GALLERY=gallery

# Notes:
# - Do NOT commit your real values in .env; .env is gitignored.
# - Supabase Edge Function secrets (e.g., RESEND_API_KEY) should be set in Supabase, not here.